# RFC Handoffs

Implementation handoffs belong here after their source RFC is accepted or after an explicit spike exception is recorded.

Handoffs inherit the lifecycle state of their source RFC. Do not create a parallel handoff status workflow.
